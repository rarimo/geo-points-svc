allOf:
  - $ref: '#/components/schemas/RarimarketAccountKey'
  - type: object
    x-go-is-request: true
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - address
        properties:
          address:
            type: string
            example: "0x123...abc"
            pattern: '^0x[0-9a-fA-F]{40}$'
            description: Account abstraction address.
