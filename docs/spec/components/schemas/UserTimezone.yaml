allOf:
  - $ref: "#/components/schemas/UserTimezoneKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - timezone
        properties:
          timezone:
            type: string
            description: timezone
          nullifier:
            type: string
            description: Nullifier of the points owner
            example: "0x123...abc"
            pattern: '^0x[0-9a-fA-F]{64}$'
